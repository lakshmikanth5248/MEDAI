# Reference only - the union of every dependency used across all 8 services
# (auth, clinical, prescription, pharmacy, billing, notification, core, gateway).
# Render does NOT install from this file - each service's own requirements.txt
# (backend/<service>/requirements.txt) is what actually gets installed per
# render.yaml, since each runs as its own deployment with its own rootDir.
Flask==3.0.3
flask-cors==4.0.1
SQLAlchemy==2.0.35
psycopg2-binary==2.9.12
alembic==1.13.3
python-dotenv==1.0.1
PyJWT==2.9.0
bcrypt==4.2.0
requests==2.32.3
gunicorn==23.0.0
-e ./shared
