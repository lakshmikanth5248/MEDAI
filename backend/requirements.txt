# Union of every dependency used across all 8 services (auth, clinical,
# prescription, pharmacy, billing, notification, core, gateway).
# render.yaml installs from THIS file and runs all 8 as subprocesses of
# start.py inside a single Render web service - see start.py.
Flask==3.0.3
flask-cors==4.0.1
SQLAlchemy==2.0.35
psycopg2-binary==2.9.12
alembic==1.13.3
python-dotenv==1.0.1
PyJWT==2.9.0
bcrypt==4.2.0
requests==2.32.3
Flask-Mail==0.10.0
twilio==9.10.9
reportlab==5.0.0
-e ./shared
